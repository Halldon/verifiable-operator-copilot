# Debate Artifacts

- `challenge-audit/latest/` contains the most recent clean-state challenge validation run.
- `runs/` is ignored in git and used for local ad-hoc executions.
- Each run folder contains signed verdict + verifiability bundle + replay command.
